I am trying to get started with Rust using this [link.](https://doc.rust-lang.org/book/ch06-02-match.html#:~:text=Rust%20has%20an%20extremely%20powerful,based%20on%20which%20pattern%20matches.)

---
While going through the Rust Book, I came across a few interesting concepts, such as lifetimes, borrowing and ownership. Note: Lifetimes are always present, but Rust can infer them automatically for some simple cases.

For now I stacked on rust rc box refcell, concetions by itself is not so hard, but need delve in this to get better understanding of uses cases.

For now, I'm stuck on Rust's `Rc`, `Box`, and `RefCell`. The concepts themselves aren't too difficult, but I need to delve into them to get a better understanding of their use cases.

For my futher reading I choose [this repo](https://github.com/instrumentisto/rust-incubator)